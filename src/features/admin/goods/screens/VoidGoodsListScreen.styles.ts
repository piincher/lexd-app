import { StyleSheet } from 'react-native';

export const createVoidGoodsListScreenStyles = () => StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  listContainer: {
    paddingBottom: 20,
  },
});
