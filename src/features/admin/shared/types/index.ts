/**
 * Admin Shared Types
 * Cross-sub-feature types that multiple admin modules depend on.
 * Import from here instead of directly from sibling sub-features.
 */

export type { Goods } from "@src/features/admin/goods/types";
export type { ShippingMode } from "@src/features/admin/containers/types";
